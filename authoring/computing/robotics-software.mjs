// Robotics software. Original descriptions; references are coverage checks only.
// Reuses existing Robotics skills (r-rigid, r-pid, r-system, r-fusion) without reclassifying them.
export const subdomain = 'Robotics software';
export const nodes = [
  { id: 'c-sensor-acquisition', name: 'Sensor data acquisition in software', icon: 'sensor', refs: ['REP103', 'ROS2', 'PICOSDK'],
    after: { 'c-io': 'Sensors are devices, read by polling, interrupts, or DMA.' },
    helpedBy: { 'c-timers': 'Hardware timers give a steady sample rate and precise timestamps.', 'c-serial-drivers': 'Many robot sensors are read over UART, SPI, or I²C, so bus drivers are usually needed in practice.', 'e-sampling': 'The sample rate must be chosen against the signal\'s frequency content to avoid aliasing.', 'e-calibration': 'Calibration curves turn raw readings into trustworthy physical values.' },
    summary: 'Read sensors at a known rate, timestamp each sample, convert raw counts to physical units, and detect stale or invalid data.',
    details: `Acquisition code turns a sensor's raw output into data the rest of the robot can trust. Samples are taken at a known, steady rate and stamped with the time they were taken, not the time they were later processed. Raw counts are converted with the scale and offset from the datasheet plus any calibration: an accelerometer set to ±2 g with a 16-bit output gives 16,384 counts per g, which is then expressed in SI units, metres per second squared, as ROS convention REP 103 specifies. The sensor's axes must also be mapped into the robot's frame with the correct signs.

Good acquisition code also reports when data cannot be trusted. It checks status bits and plausible ranges, marks a sensor as stale when no new sample has arrived for a few periods and passes that fault on rather than repeating the last value forever. Samples are buffered so short processing delays lose nothing, and any filtering is applied deliberately, with its delay understood. You can read an IMU at 200 Hz, convert its outputs to m/s² and rad/s in the robot's frame and flag a disconnected sensor within 20 ms.` },
  { id: 'c-transforms', name: 'Coordinate transforms in software', icon: 'arm', refs: ['ROS2', 'REP103', 'REP105'],
    after: { 'r-rigid': 'Code can only compose transforms correctly once rigid transformations are understood mathematically.', 'c-functions': 'Transform code is organized as functions that compose, invert, and apply poses.' },
    helpedBy: { 'm-tree': 'A robot\'s frames form a tree in which each frame has one parent, which is how transform libraries store them.', 'c-floats': 'Rounding error slowly denormalizes rotations, so they must be renormalized.' },
    summary: 'Represent poses and frames in code, compose and invert transforms in the correct order, and look them up from a time-stamped transform tree.',
    details: `A pose is a position plus an orientation, and in code the orientation is usually a rotation matrix or a unit quaternion. Libraries disagree on quaternion component order, (w, x, y, z) versus (x, y, z, w), which causes silent errors. Euler angles are convenient to read but poor for computing, because conventions differ and they suffer from gimbal lock. A transform is stored as a 4×4 homogeneous matrix or as a rotation and translation pair. Naming variables by their frames, such as T_map_base for the transform that takes points from the base frame into the map frame, makes the composition order visible: T_map_sensor = T_map_base · T_base_sensor, and a rigid transform's inverse is (Rᵀ, −Rᵀt).

ROS conventions fix the details: REP 103 uses right-handed frames with x forward, y left and z up, and REP 105 defines the map, odom and base_link frames. The tf2 library keeps every frame in a tree, with one parent per frame, and buffers its history, so a measurement is transformed using the robot's pose at the time the measurement was taken. Rotations accumulated from many small updates must be renormalized. You can move a lidar point into the map frame through two transforms, check the result with the inverse and find a sign error caused by mixed conventions.` },
  { id: 'c-messaging', name: 'Publish-subscribe messaging and robot middleware', icon: 'network', refs: ['ROS2'],
    after: { 'c-event-loops': 'Middleware delivers messages by running callbacks from an event loop, called an executor in ROS 2.' },
    helpedBy: { 'c-framing': 'Middleware serializes and frames messages for you, but knowing how explains message size and compatibility.', 'c-transport': 'Reliability settings map onto the delivery guarantees of the transport underneath.' },
    summary: 'Connect robot software components through typed messages on topics, request-response services, and quality-of-service settings.',
    details: `Robot middleware such as ROS 2, and messaging systems such as MQTT, decouple software components. A publisher sends typed messages on a named topic and any number of subscribers receive them, without either side knowing the other. A service is a request and response, and in ROS 2 an action is a long-running goal, such as navigating to a pose, with progress feedback and cancellation. Message definitions fix the fields and imply their units and frames, and a header timestamp says when the data was measured.

Quality-of-service settings decide how delivery behaves: reliable or best-effort, how many past messages are kept, whether a late-joining subscriber receives the last message (useful for a map) and what deadlines apply. Incompatible settings between a publisher and a subscriber mean no data flows, and the only sign is an incompatible-QoS warning that is easy to miss, a common source of "the topic exists but I receive nothing". Discovery across machines depends on network settings such as the ROS domain ID. You can design topics, types and QoS for a mobile robot's odometry, velocity commands and laser scans, choose between a topic, a service and an action for a task, and diagnose a QoS mismatch.` },
  { id: 'c-state-machines', name: 'Finite-state machines for robot behaviour', icon: 'robot', refs: ['ROS2LIFECYCLE', 'BTCPP', 'MIT6004'],
    after: { 'c-loops': 'A state machine is stepped repeatedly inside a program\'s loop.', 'c-functions': 'Entry actions, exit actions, and transition handlers are functions.' },
    helpedBy: { 'm-graph': 'A state diagram is a directed graph whose vertices are states and whose edges are transitions.' },
    related: { 'e-latch': 'Latches and flip-flops hold state in hardware; sequential logic circuits are hardware state machines.' },
    summary: 'Model robot behaviour as explicit states, events, and transitions, and implement the machine so every state handles every relevant event deliberately.',
    details: `A finite-state machine describes behaviour as a small set of states, such as IDLE, HOMING, RUNNING and FAULT, the events that can occur, such as a button press, a target reached or an error, and the transitions each event causes in each state, optionally guarded by conditions and with actions on entry and exit. In code it is an enumeration of states with a switch statement, or a table mapping state and event to the next state. It replaces tangles of Boolean flags whose combinations nobody has thought through.

The discipline is to decide, for every state, what happens on every event that matters, especially faults: an emergency stop must lead to a safe state from anywhere. States must not block with long waits, so the loop stays responsive and can still react to faults. Larger behaviours use hierarchical state machines or behaviour trees, and ROS 2's managed lifecycle nodes, with unconfigured, inactive, active and finalized states, are a standard state machine for starting and stopping components. You can implement a gripper controller with open, closing, holding and fault states, test every transition and show that an emergency stop is handled in every state.` },
  { id: 'c-control-loop', name: 'Implementing a discrete-time control loop', icon: 'feedback', refs: ['ROS2CTRL', 'FBS'],
    after: { 'r-pid': 'The loop implements a controller whose proportional, integral, and derivative behaviour must already be understood.', 'c-sensor-acquisition': 'Each cycle starts from timestamped, validated measurements.' },
    helpedBy: { 'c-timing-constraints': 'A loop that overruns its period changes the controller\'s behaviour, so its timing budget must be checked.', 'm-euler': 'Accumulating the integral term once per cycle is a forward Euler step.', 'e-pwm': 'The controller\'s output usually becomes a PWM duty cycle.' },
    summary: 'Implement a feedback controller as a fixed-rate read, compute, and write loop with correct time steps, output limits, anti-windup, and safe start and stop behaviour.',
    details: `A software controller runs a cycle at a fixed rate: read the sensors, compute the new output, write it to the actuators. ros2_control organizes robot hardware around exactly this read, update and write cycle. Continuous PID becomes discrete: the integral accumulates error × dt each cycle, and the derivative uses the change in a filtered measurement rather than the error, which avoids a spike when the setpoint jumps. The time step is the loop's actual period, measured rather than assumed when timing varies, and all quantities use consistent units.

The details that make a controller safe are outside the textbook equation. Outputs are clamped to actuator limits, and integration pauses while the output is saturated, so the integral does not wind up and cause overshoot afterwards. The first cycle has no previous measurement, a missing or stale sensor sample leads to a safe output instead of a guess, and overruns are detected and counted. Setpoint, measurement and output are logged for tuning, gains are first tried in simulation and at reduced levels, and the loop rate is set well above the bandwidth the controller must achieve. You can implement a 500 Hz motor speed loop, show that its integrator does not wind up during saturation and handle a lost encoder sample.` },
  { id: 'c-simulation', name: 'Robot simulation and simulated time', icon: 'motion', refs: ['GAZEBO', 'ROS2'],
    after: { 'c-loops': 'Simulation and the software under test run as stepping loops.' },
    helpedBy: { 'm-euler': 'Knowing how a fixed time step advances a state explains why the physics step size affects accuracy and stability.', 'r-dynamics': 'Simulators integrate a dynamic model of the robot, so its assumptions carry over.' },
    summary: 'Run robot software against a simulator, handling simulated time, the physics step, and the differences between simulation and the real robot.',
    details: `A robot simulator such as Gazebo advances physics in fixed steps, often a millisecond, generates simulated sensor data and publishes its own clock. When the simulation runs slower than real time, a real-time factor below 1, software must follow simulated time, which ROS 2 nodes do with use_sim_time, or its timers and timeouts misbehave. Fixed random seeds make runs repeatable. Bridges such as ros_gz_bridge connect simulated sensors and actuators to the same topics the real robot uses, so the same software can run on both.

Simulation is a model, and friction, contact, motor limits, delays and sensor noise are all approximations. Behaviour that works only in simulation often fails on hardware, the sim-to-real gap, so realistic noise, delays and limits are added on purpose. It is most valuable for trying situations that would be dangerous or expensive to create on hardware. You can run a controller against a simulated robot using simulated time, explain what breaks when a node uses the wall clock at a real-time factor of 0.5 and list differences to expect on hardware.` },
  { id: 'c-logging', name: 'Logging and recorded data', icon: 'data', refs: ['ROS2', 'PYTUT'],
    after: { 'c-files': 'Logs and recordings are written to files that must be managed.' },
    helps: { 'c-debugging': 'Recorded logs and data let a fault be investigated after it has happened.' },
    summary: 'Record time-stamped logs and data streams at a useful level of detail, and replay them to diagnose problems after the fact.',
    details: `Logs record what software did and why. Levels such as DEBUG, INFO, WARNING and ERROR let each module be made quieter or more detailed without code changes, and each entry should carry a timestamp, the component and the values that matter, such as "left_wheel_speed=0.42 m/s", rather than vague prose. Logging has a cost: writing a line on every cycle of a fast loop can itself cause overruns, so high-rate data is rate-limited, kept in a ring buffer and written out on a fault, or moved off the real-time thread.

Robot problems are often intermittent, so recording raw data streams, for example with ROS 2's rosbag2, allows a run to be replayed and a bug reproduced at a desk. A recording is most useful with its context: software version, parameters, calibration and clock source. Logs need rotation so they do not fill the disk, and camera or microphone recordings may contain personal data. You can set log levels per module, record a run's sensor topics, replay them to reproduce a fault offline and explain why excessive logging made a control loop miss its deadline.` },
  { id: 'c-time-sync', name: 'Timestamps and clock synchronization', icon: 'clock', refs: ['RFC7679', 'ROS2', 'CS2023'],
    after: { 'c-sensor-acquisition': 'Synchronization starts from timestamps taken at acquisition.' },
    helpedBy: { 'c-latency': 'Compensating for delay requires knowing how old each measurement is when it arrives.' },
    helps: { 'r-fusion': 'Fusion must combine measurements taken at different moments by devices with different clocks.' },
    summary: 'Use the right clock for each purpose, timestamp data at its source, and align data from devices whose clocks disagree.',
    details: `Computers have several clocks. Wall-clock time can jump when it is corrected or changed, so durations and timeouts use a monotonic clock, and simulations use simulated time. Every device's oscillator also drifts: two clocks each accurate to ±20 ppm can disagree by up to 0.144 s after an hour, so timestamps from different devices cannot be compared naively. Protocols reduce the offsets: NTP typically achieves milliseconds over a network, PTP (IEEE 1588) reaches sub-microsecond accuracy with hardware support, and a shared trigger wire can make sensors sample at the same instant.

Once timestamps are trustworthy, streams can be aligned by interpolating each to common times, and delays can be compensated: a camera image that is 40 ms old when it arrives must be combined with the robot's pose 40 ms ago, not now, which is why transform libraries look poses up by timestamp. You can pick the correct clock for a timeout, a log entry and a sensor sample, estimate drift between two devices and align an IMU and a camera stream by timestamp.` },
  { id: 'c-replay-testing', name: 'Testing with simulation and recorded data', icon: 'robot', refs: ['ROS2', 'GAZEBO', 'PYUNITTEST'],
    after: { 'c-testing': 'System-level tests extend the assertions and test runners of unit testing.', 'c-simulation': 'Scenario tests run the software against a simulated robot.', 'c-logging': 'Replay tests feed recorded sensor data back through the software.' },
    summary: 'Test robot software automatically against simulated scenarios and replayed sensor recordings before it runs on hardware.',
    details: `Robot software is tested in layers. Unit tests check individual functions; component tests replay a recorded run through one piece of software, such as localization, and compare its output with expected results within tolerances; scenario tests run the whole stack in simulation; hardware-in-the-loop tests connect the real controller to a simulated robot; and only then comes testing on the real machine. Replays and simulations must be deterministic, using simulated time and fixed seeds, or failures cannot be reproduced.

The most valuable tests inject faults deliberately: dropped messages, extra delay, a sensor that freezes or publishes nonsense, and a lost network link, each with an assertion about the safe response. Run in continuous integration, these tests catch regressions before they reach the robot. You can build a test that replays a recording through a node and checks its error bounds, and a simulated scenario in which the lidar stops publishing and the test asserts that the software reports the sensor as failed within a set time.` },
  { id: 'c-integration', name: 'Robot software integration and bring-up', icon: 'robot', refs: ['ROS2', 'REP105'],
    after: { 'c-messaging': 'Components are joined through topics, services, and actions.', 'c-transforms': 'Every sensor and actuator must be placed correctly in the robot\'s frame tree.' },
    helpedBy: { 'c-state-machines': 'System behaviour and start-up sequencing are easier to control as explicit state machines.', 'c-logging': 'Integration faults are found through logs and recordings.' },
    helps: { 'r-system': 'Software integration is one of the subsystems that robot system integration must coordinate.' },
    summary: 'Assemble sensing, control, and communication software into one working robot through launch configuration, parameters, interface checks, and staged bring-up.',
    details: `Integration turns separately working components into a working robot. Launch files start the nodes with their parameters and topic remappings, and parameters kept in configuration files separate tuning from code and record exactly what ran. Most integration failures are interface mismatches: a topic name, message type, unit, frame, rate or QoS setting that two components disagree on. Writing these interfaces down, and checking them, prevents most of that pain.

Bring-up proceeds in stages: power and communication first; then each sensor, checked for correct values and frames in a visualization tool such as RViz; then actuators with the wheels off the ground and speeds limited; then closed-loop control; and only then autonomy. An emergency stop that works independently of the software is in place from the start, health monitoring reports stale or failed components, and software, firmware and parameter versions are recorded together. You can write a launch configuration for sensor, controller and teleoperation nodes, produce a bring-up checklist and diagnose a robot that spins because its IMU frame is mounted rotated.` }
];
