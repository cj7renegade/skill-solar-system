// Machine-learning foundations for robotics. Original descriptions; references are coverage checks only.
// Reuses existing Mathematics skills (m-sampling, m-regression, m-gradient, m-matrix, m-prob, m-spread).
export const subdomain = 'Machine learning foundations';
export const nodes = [
  { id: 'c-datasets', name: 'Datasets, features, and labels', icon: 'data', refs: ['MLCC', 'SKLEARN'],
    after: { 'm-sampling': 'A useful dataset is a representative sample, and splits rely on sampling without bias.', 'c-collections': 'Examples, features, and labels are handled in code as tables and collections.' },
    helpedBy: { 'c-files': 'Datasets are loaded from and saved to files.' },
    summary: 'Organize data into examples with features and labels, split it into training, validation, and test sets, and spot leakage and bias.',
    details: `A dataset is a set of examples. Each example has features, the inputs a model sees, such as statistics of a one-second IMU window, and for supervised learning a label, the answer it should learn, such as the terrain type. Quality decides everything downstream: the data must represent the conditions the robot will meet, including surfaces, lighting, sensor models and speeds; labels must be checked for mistakes; and class balance must be known, because if 95 % of examples are "normal", a model that always says "normal" scores 95 % accuracy while being useless.

Data is split into a training set for fitting, a validation set for choosing settings and a test set used once at the end for an honest estimate. Leakage, where information about the test data seeps into training, makes results look better than they are. With robot recordings, neighbouring frames are almost identical, so a random split puts near-copies on both sides; splitting by whole runs or by time prevents this. You can split recorded runs so no run appears in two sets, report class proportions and spot a feature, such as a timestamp, that gives away the label.` },
  { id: 'c-features', name: 'Feature scaling and preprocessing', icon: 'data', refs: ['MLCC', 'SKLEARN'],
    after: { 'c-datasets': 'Preprocessing is fitted on the training split of a dataset.', 'm-spread': 'Standardization divides by the standard deviation.' },
    summary: 'Transform raw inputs into model-ready features by scaling, encoding categories, and handling missing values, using statistics from the training set only.',
    details: `Many models are sensitive to the scale of their inputs: a distance measured in millimetres swamps one measured in metres in any distance-based method, and gradient descent converges slowly on badly scaled features. Standardization replaces each value with (x − mean) / standard deviation; min-max scaling maps values into a fixed range. Categories such as surface type become one-hot vectors, and missing values are removed, filled in or flagged with an extra indicator feature. Raw signals are often summarized first, for example as the mean, variance or dominant frequency over a time window.

The statistics used for scaling are computed on the training data only and then applied unchanged to validation, test and live data; computing them on everything leaks information. Keeping the steps in one pipeline object means they are always fitted and applied together, in the same order. You can standardize features without leakage, apply a fitted scaler to new data and explain why one unscaled feature dominated a nearest-neighbour classifier.` },
  { id: 'c-train-inference', name: 'Training versus inference', icon: 'model', refs: ['MLCC', 'LITERT'],
    after: { 'c-datasets': 'Training fits a model to a dataset.', 'm-function': 'A trained model is a function whose parameters were chosen from data.' },
    summary: 'Distinguish fitting a model\'s parameters to data during training from applying the fixed parameters to new inputs during inference, with their different costs and failure modes.',
    details: `A model is a function with adjustable parameters. Training searches for parameter values that minimize a loss measuring the error on labelled training data; it is iterative, often expensive and usually done offline on a desktop computer or GPU. Inference applies the trained function, with its parameters frozen, to new inputs such as live sensor data; it needs no labels, but it must fit the robot's time, memory and power budgets and may run thousands of times per second.

Deployment ships more than the weights: the preprocessing steps, the feature definitions, the label names and the version of all of them. When inference preprocessing differs from training, the model silently receives inputs it never learned, a problem called training-serving skew. You can say what is computed, where and how often in each phase for a terrain classifier, list everything that must be deployed with the model and compare its inference time with a control period.` },
  { id: 'c-ml-regression', name: 'Regression models and loss functions', icon: 'model', refs: ['MLCC', 'SKLEARN'],
    after: { 'm-regression': 'Fitting a line to data and reading residuals is the statistical basis of a regression model.', 'c-train-inference': 'Regression is trained by minimizing a loss and then used for inference.' },
    helpedBy: { 'm-leastsquares': 'Minimizing squared error for a linear model has a closed-form least-squares solution.' },
    summary: 'Fit a model that predicts a continuous value by minimizing a loss such as mean squared error, and judge it on held-out data.',
    details: `Regression predicts a number, such as the remaining battery time from current draw and temperature, or wheel slip from motor commands. A linear model predicts ŷ = w·x + b over several features, and training chooses w and b to minimize a loss. Mean squared error penalizes large errors heavily and has a closed-form least-squares solution for linear models; mean absolute error is more tolerant of outliers. Nonlinear relationships are handled with transformed features such as powers or with nonlinear models.

A model is judged on validation data, not on the data it was fitted to, and against a baseline such as always predicting the mean; a model that barely beats that baseline has learned little. The root-mean-square error is reported in the target's own units, for example minutes of battery life, so its practical meaning is clear, and predictions outside the range of the training data should not be trusted. You can fit a battery-time model, report its validation RMSE against a mean baseline and explain what squared versus absolute error would emphasize.` },
  { id: 'c-gradient-descent', name: 'Gradient-descent training', icon: 'model', refs: ['MLCC'],
    after: { 'm-gradient': 'Each update steps against the gradient of the loss with respect to the parameters.', 'c-ml-regression': 'A regression loss is the simplest function to minimize this way.' },
    helpedBy: { 'c-features': 'Well-scaled features let gradient descent converge with a single learning rate.' },
    summary: 'Train model parameters by repeatedly stepping against the gradient of the loss, choosing the learning rate, batch size, and number of passes over the data.',
    details: `Gradient descent improves parameters step by step. The gradient of the loss points in the direction of steepest increase, so each update moves the parameters a small step the other way: θ ← θ − η∇L, where η is the learning rate. Too large a rate makes the loss oscillate or blow up; too small a rate makes progress painfully slow. Computing the gradient on a random mini-batch of examples instead of the whole dataset gives noisy but cheap steps, and one pass through all the training data is an epoch.

Plotting training and validation loss against epochs shows whether training is converging, has stalled or has started to overfit, which is the signal for early stopping. Refinements such as momentum and Adam adapt the step size automatically, and poorly scaled features make the problem much harder. You can implement gradient descent for linear regression by hand, plot the loss for three learning rates and explain why the largest one diverges.` },
  { id: 'c-classification', name: 'Classification and decision thresholds', icon: 'model', refs: ['MLCC', 'SKLEARN'],
    after: { 'c-train-inference': 'A classifier is trained on labelled examples and then used for inference.', 'm-prob': 'Classifiers output probabilities that must be interpreted and thresholded.' },
    summary: 'Train models that assign inputs to categories, interpret their predicted probabilities, and choose a decision threshold by the cost of each kind of mistake.',
    details: `A classifier assigns each input to a category: stairs or no stairs, or one of several terrain types. Logistic regression passes a weighted sum through the sigmoid function to produce a probability, and a softmax output does the same across several classes; training minimizes cross-entropy, or log loss. The model's decision boundary separates the regions of feature space assigned to each class.

A probability becomes a decision through a threshold, and 0.5 is only a default. When a missed obstacle is far worse than a false alarm, the threshold for "obstacle" should be lower, accepting more false alarms. Predicted probabilities are not necessarily calibrated, so a score of 0.8 does not guarantee an 80 % chance unless calibration has been checked, and rare classes need special care. You can train a terrain classifier on IMU features, choose a threshold for a safety-relevant class given the costs of each error and explain what calibration means.` },
  { id: 'c-evaluation', name: 'Evaluating models with metrics and held-out data', icon: 'data', refs: ['SKLEARN', 'MLCC'],
    after: { 'c-classification': 'Confusion matrices, precision, and recall evaluate classifiers.', 'c-ml-regression': 'Error measures such as MAE and RMSE evaluate regression models.' },
    summary: 'Measure model performance on data it has not seen with suitable metrics, including confusion matrices, precision and recall, and regression errors.',
    details: `A model is evaluated on data it was not trained on. For classification, a confusion matrix counts true positives, false positives, false negatives and true negatives. Precision, TP / (TP + FP), says how many alarms were real; recall, TP / (TP + FN), says how many real cases were caught; and the F1 score combines them. Accuracy alone misleads when classes are imbalanced. Curves of precision against recall across thresholds show the available trade-offs, and for regression MAE and RMSE state typical error in the target's units.

Estimates from small test sets are noisy, and k-fold cross-validation uses limited data more fully, with each fold made of whole recording sessions for the same reason that dataset splits keep whole runs apart. Results should be broken down by condition, such as day and night or carpet and tile, because an average can hide a condition where the model fails completely, and should always be compared with a simple baseline. You can compute precision and recall from a confusion matrix, choose the right metric for an obstacle detector and run grouped cross-validation.` },
  { id: 'c-overfitting', name: 'Overfitting, underfitting, and regularization', icon: 'model', refs: ['MLCC', 'SKLEARN'],
    after: { 'c-evaluation': 'Overfitting is diagnosed by comparing training performance with held-out performance.' },
    helpedBy: { 'c-gradient-descent': 'Early stopping uses the validation loss curve recorded during gradient-descent training.' },
    summary: 'Diagnose overfitting and underfitting from training and validation error, and control model complexity with regularization, more data, and early stopping.',
    details: `A model underfits when it is too simple to capture the pattern, so both training and validation error stay high. It overfits when it learns the noise and quirks of its training data: training error keeps falling while validation error rises. Plotting both errors against model complexity, such as polynomial degree or network size, or against the amount of training data, shows which problem you have.

Overfitting is reduced with more and more varied data, data augmentation, simpler models, regularization such as an L2 penalty on large weights or dropout in neural networks, and early stopping when validation loss stops improving. Repeatedly tuning against the validation set gradually overfits that set as well, so a final check on untouched data is still needed. You can fit polynomials of increasing degree, identify where overfitting starts from the error curves and show that regularization improves validation error.` },
  { id: 'c-neural-networks', name: 'Neural-network basics', icon: 'model', refs: ['MLCC'],
    after: { 'c-gradient-descent': 'Networks are trained by gradient descent on gradients computed by backpropagation.', 'm-matrix': 'A layer computes a matrix-vector product followed by an activation function.' },
    helpedBy: { 'c-classification': 'Classification networks end in a softmax layer trained with cross-entropy.' },
    summary: 'Explain how a neural network computes outputs through layers of weighted sums and nonlinear activations, and how backpropagation trains it.',
    details: `An artificial neuron computes a weighted sum of its inputs plus a bias and passes it through a nonlinear activation function such as ReLU, max(0, x). A layer applies many neurons at once, which is a matrix-vector product plus a bias vector followed by the activation. Stacking layers lets a network represent complicated nonlinear functions, whereas without activations any stack of layers collapses into a single linear map. The output layer is linear for regression or a softmax for classification. A fully connected layer with 64 inputs and 32 outputs has 64 × 32 weights plus 32 biases, 2,080 parameters in all.

Training runs a forward pass to compute the loss, uses backpropagation, the chain rule applied layer by layer, to find the gradient for every parameter, and then applies a gradient-descent update. Convolutional layers reuse small filters across an image, which suits camera data. Networks need large amounts of data and careful choices of architecture, learning rate and regularization. You can compute the forward pass of a tiny network by hand, count the parameters in a layer and explain why nonlinear activations are essential.` },
  { id: 'c-model-deployment', name: 'Deploying learned models under robot constraints', icon: 'model', refs: ['LITERT', 'MLCC'],
    after: { 'c-train-inference': 'Deployment concerns the inference phase and everything shipped with the trained model.', 'c-evaluation': 'Deployment decisions compare a compressed model\'s measured accuracy against the original.' },
    helpedBy: { 'c-neural-networks': 'Model size and compute cost depend on the network\'s architecture.', 'c-fixed-point': 'int8 quantization is fixed-point arithmetic applied to weights and activations.', 'c-timing-constraints': 'Inference must fit inside the deadline of the loop that uses it.' },
    summary: 'Fit a trained model into a robot\'s compute, memory, latency, and power limits, and monitor it for inputs unlike its training data.',
    details: `On a robot, a model must produce its answer within a deadline, on the available processor, within limited memory and power. Microcontroller inference libraries such as LiteRT for Microcontrollers run small models in tens of kilobytes without an operating system or dynamic allocation, while larger models need a single-board computer or an accelerator. Quantization to 8-bit integers makes a model about four times smaller and usually faster, at a small cost in accuracy that must be measured using representative calibration data; pruning and smaller architectures are other options. Latency is measured on the target hardware under realistic load, worst case included.

Deployed models meet conditions their training data never covered, called distribution shift: new surfaces, weather or a replaced sensor. Low confidence or out-of-range inputs should be detected and logged, and learned outputs should pass through plausibility checks, with a conventional fallback behaviour that does not depend on the model. Models are versioned so that a bad update can be rolled back. You can quantize a model and compare accuracy and latency on the target, decide whether a 15 ms model fits a 50 Hz loop alongside other work and define a fallback for low-confidence outputs.` }
];
